{
  "name": "voxelier",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "A lo-fi voxel scene editor (1 voxel = 1 cm).",
  "scripts": {
    "build": "node build.mjs",
    "dev": "node build.mjs --watch",
    "lint": "eslint src",
    "check": "node selfcheck.mjs",
    "test": "node selfcheck.mjs"
  },
  "dependencies": {
    "three": "0.160.0"
  },
  "devDependencies": {
    "esbuild": "^0.24.2",
    "eslint": "^9.17.0"
  },
  "pnpm": {
    "onlyBuiltDependencies": ["esbuild"]
  }
}
